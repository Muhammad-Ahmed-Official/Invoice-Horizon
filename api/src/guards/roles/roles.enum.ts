export enum Role {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum clientType {
  CUSTOMER = "CUSTOMER",
  VENDOR = "VENDOR",
}

export enum statusType {
  PAID = "PAID",
  PENDING = "PENDING",
  OVERDUE = "OVERDUE"
}