import { ResponseInterceptorFilter } from './response-interceptor.filter';

describe('ResponseInterceptorFilter', () => {
  it('should be defined', () => {
    expect(new ResponseInterceptorFilter()).toBeDefined();
  });
});
